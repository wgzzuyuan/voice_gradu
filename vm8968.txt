
1，PCM和wav文件格式的关系？？？

最基本的WAVE文件是PCM（脉冲编码调制）格式的，这种文件直接存储采样的声音数据没有经过任何的压缩，
是声卡直接支持的数据格式，要让声卡正确播放其它被压缩的声音数据，就应该先把压缩的数据解压缩成PCM
格式，然后再让声卡来播放


2，wav 文件转换成数组后的结构是什么？？？或者说wav文件的格式是什么？？？


3，I2S几个引脚？？？几根线？？？

4个引脚，2根数据线

4，







